#Application properties
server:
  port: 8081


#AWS properties
cloud:
  aws:
    region:
      static: ap-south-1
    stack:
      auto: false
    credentials:
      accessKey:
      secretKey:

application:
  bucket:
    name: s3storage1

logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: error
